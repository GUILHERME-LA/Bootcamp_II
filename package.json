{
  "name": "lembrete-agua-extensao",
  "version": "1.0.0",
  "description": "Extensão Chrome MV3 para lembrete de água com Playwright E2E.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "test:e2e": "playwright test --reporter=list,html --host 0.0.0.0 --port 9324",
    "test": "npm run build && npm run test:e2e",
    "ci": "npm ci && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "archiver": "^7.0.0"
  }
}